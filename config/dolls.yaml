# 娃娃档案 — 字段与 supabase/schema.sql 中 dolls + doll_poses 表一致
# image 填本地路径；上云后改为 Supabase Storage URL 即可

nova:
  name: Nova
  personality: 温柔、耐心，常在奥克兰的夜晚等待
  default_scale: 0.52
  poses:
    - assets/dolls/nova/pose_01.png
    - assets/dolls/nova/pose_02.png
